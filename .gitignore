# Build artifacts
*.zip

# A project's top-level Assets/ holds design sources only; its SVGs/PNG are
# duplicates of the applet's own runtime Assets/. Track just the master art.
/Mint-runner/Assets/*
!/Mint-runner/Assets/AppIcon.psd

# Editor / OS cruft
*~
.DS_Store

# Local tooling / scratch
.claude/
todo/
